class DOMHelper {
  constructor() {
    console.log('DOMHelper loaded');
  }

  /**
   *
   * @param {*} domNode node on which to append rendering content
   * @param {*} className
   */
  static render(domNode, className) {
    console.log('Called DOMHelper.render');
  }
}
